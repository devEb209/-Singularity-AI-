# RRW chain — Genesis success path

Complement to [RRW.md](./RRW.md) and the Tese dos D. It does **not** replace either.

The user-facing success definition of Genesis is an executing path, not a slide:

```text
1. describe
2. understand intent
3. seek / build knowledge
4. represent via RRW
5. D-O15 chooses description
6. materialize
7. run on different hardware
8. persist / resume the same world
9. verify
10. refine automatically
```

Time is part of that reality. Season is axial tilt + insolation, not a skybox. Weather is pressure/wind/moisture coupling, not a particle shader. Hydrology conserves H₂O moles (ocean ↔ vapor ↔ cloud ↔ rain ↔ soil ↔ river). Tide is lunar height on the ocean extent. Soil transfers cellulose without a vegetation texture. Society work is counted across the whole day span, not the last hour.

D-O15 still presents the **same node IDs** on ancient and dedicated devices, with weaker hardware using lower description levels. Dormant packets reconstruct from the held graph. They are not meshes.

```text
POST /api/v1/rrw/chain/build
```

Honest limit: the chain executes. It is **not** complete reality, not NIST-measured spectra, not a shipped AAA world. `genesisClosed: false`.
