# V1 Remaining Gaps Only

V1 is the first generation and must compete when launched. Remaining items below are **V1 work**, not a postponed final. See [V1-FIRST-GENERATION.md](./V1-FIRST-GENERATION.md).

This registry intentionally excludes completed capabilities. Canonical API:

```text
GET /api/v1/v1-gaps
GET /api/v1/v1-gaps?area=SNB
GET /api/v1/v1-gaps?area=UES
GET /api/v1/v1-gaps?area=DsOS
GET /api/v1/v1-gaps?area=SNB%20Infrastructure
```

Statuses are limited to `PARTIAL`, `ADAPTER_REQUIRED`, `INFRASTRUCTURE_REQUIRED`, `BLOCKED`, and `PLANNED`. No `ACTIVE` record is permitted in this list.

The SNB Master Intelligence now compiles intent, complexity, context, temporary specialists, three candidate realities, contradictions, risks, experiments, critics and a persistent mission/artifact across all 30 scoped cognitive programs. Remaining SNB work is real specialist execution with correction receipts, transactional graph mutation, semantic/versioned memory, universal automation/plugins/documents, offline sync and product-side GitHub authorization.

Major remaining UES gaps are learned image-to-3D / specialist-derived scans, millions of unique full NMN agents, complete OBB-OBB rotational CCD / spatial branched ABA, hardware GPU presentation (WebGPU/Vulkan device), GPU SPH / vendor shaders, real WebRTC multiplayer, codecs, measured HRTF, vision/video motion analysis, live NASA/GIS/photoreal 3D Tiles ingest and GPU frame telemetry. RRW now executes as the Genesis foundation (intent/compose/present/critic/history/hold/days/society/walk). The close loop is real and still incomplete. Compatibility radiance/PBR remains a port, not the architecture. RRW is not complete reality, not NIST spectra, not always-on atomic simulation, and does not beat Unreal at Unreal’s game. Edge-flow remesh, anatomy templates, simulated net authority, reconstruction filters, frame interpolation, GJK/EPA, analytic sphere/capsule CCD, yaw vertex-arc CCD, Featherstone CRBA+RNEA planar serial, CPU Stable Fluids, voxel navmesh from arbitrary/CSG meshes, hierarchical 1024/64 city lives with climate/needs, remote studio graph/timeline/undo, multi-round consensus HMAC receipts, 96-agent city census with identity-preserving region swap, structured reference rights, motion matching, ITD/ILD loop seams, nine-kind semantic corpus plus open-class compiler, geometry/anatomy critics, particle constraints, iterative kinematic CCD, CPU PSNR/SSIM regression, chunk hysteresis streaming, Earth-like planetary geophysics with D-O15 partitions, internal GIS fixture + own 3D Tiles HLOD, FNWS heightfield water with licensed-layer ingest, TITKO PBR prompt graphs, Explorer-applied structured motion, planet heightfield nav and world synthesis now exist as CPU reference systems. They do not reconstruct the physical Earth from NASA feeds, do not require Cesium, and do not store 16K bitmaps. The verified UES-owned runtime executes world chunks, fixed-step physics, normalized skin weights, animation keyframes, NPC ticks, particles, audio synthesis and safe deduplication. The advanced internal pipeline additionally executes semantic part geometry, sweep-and-prune broadphase, AABB impulses, friction, rotational integration, joints, triggers, raycasts, FK, iterative IK, retargeting, LOD and structured critics. These implemented capabilities are excluded from the remaining advanced work.

The HSDS local baseline is implemented and therefore is not a gap: persisted sessions execute verified SVG visual-state capture → SSE playback → authenticated input → next-frame feedback with adaptive profiles in the UES Stage. Only high-fidelity GPU audiovisual HSDS remains, because it requires an external render/encoder/WebRTC worker. See [HSDS.md](./HSDS.md).

Major remaining DsOS gaps are isolated source checkout, reproducible rootfs/image build, QEMU boot verification, Android toolchain/emulator/signing, legal compatibility runtime, hardware/driver evidence and package trust.

Public HTTPS/e-mail, optional target engines, remote object storage, connected browser matrices and arbitrary-code microVM execution are tracked separately as non-blocking external validation/deployment gates. They no longer reduce internal V1 completion; local/self-hosted capabilities and disabled unsafe execution remain the V1 behavior.
