# Autonomous Benchmark Scientist

The scientist analyzes stored model evaluation count, success rate, score mean/dispersion and p95 latency, identifies unmeasured weaknesses, generates capability-specific normal and adversarial cases, and runs a deterministic proposal critic. A verified proposal is a versioned project artifact; it is not automatically promoted into a benchmark suite or model ranking until a human/validated campaign adopts and executes it.
