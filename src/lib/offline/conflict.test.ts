import{describe,expect,it}from'vitest';import{mergeDisjoint}from'./conflict';describe('client conflict merge',()=>{it('merges disjoint fields and refuses overlap',()=>{expect(mergeDisjoint({name:'server'},{count:2},['name'],['count']).value).toEqual({name:'server',count:2});expect(mergeDisjoint({name:'server'},{name:'client'},['name'],['name'])).toMatchObject({conflict:true,fields:['name']})})})
