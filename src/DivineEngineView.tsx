import { useCallback, useEffect, useMemo, useRef, useState, type ChangeEvent } from 'react'
import { Activity, Box, CheckCircle2, ChevronRight, Cloud, Cpu, FileUp, Gem, Globe, Layers3, Lock, MessageSquareText, Monitor, Paperclip, Package, Play, Plus, Radio, RefreshCw, Search, Send, Server, Settings2, ShieldCheck, Smartphone, TriangleAlert, Users, WandSparkles, X } from 'lucide-react'
import { api, type FileRecord } from './lib/api'

type Props={notify:(message:string)=>void}
type DivineSummary={id:string;name:string;brief:string;target:string;status:string;missionStatus:string;progress:number;artifactCount:number;bosses:{total:number;completed:number;running:number;failed:number}}
const detectDevice=()=>{const nav=navigator as Navigator&{deviceMemory?:number;connection?:{effectiveType?:string;saveData?:boolean}},cores=nav.hardwareConcurrency||1,memoryGB=nav.deviceMemory,width=innerWidth,effectiveType=nav.connection?.effectiveType??'unknown',tier=memoryGB!==undefined&&memoryGB<=2||cores<=2||width<480?'low':memoryGB!==undefined&&memoryGB>=8&&cores>=8&&width>=1200?'high':'balanced';return{tier,cores,memoryGB:memoryGB??'unknown',viewport:{width:innerWidth,height:innerHeight,devicePixelRatio},network:{effectiveType,saveData:nav.connection?.saveData??false},detectedAt:new Date().toISOString()}}

export default function DivineEngineView({notify}:Props){
 const[projects,setProjects]=useState<DivineSummary[]>([]),[selected,setSelected]=useState<any>(),[name,setName]=useState(''),[brief,setBrief]=useState(''),[target,setTarget]=useState('web'),[creating,setCreating]=useState(false),[prototyping,setPrototyping]=useState(false),[registry,setRegistry]=useState<any>({settings:[],presets:[],summary:{}}),[settings,setSettings]=useState<any>({values:{},preset:'custom'}),[settingSearch,setSettingSearch]=useState(''),[settingCategory,setSettingCategory]=useState('rendering'),[visibleLimit,setVisibleLimit]=useState(20),[commands,setCommands]=useState<any[]>([]),[message,setMessage]=useState(''),[attachmentIds,setAttachmentIds]=useState<string[]>([]),[previewUrl,setPreviewUrl]=useState<string>(),[hsds,setHsds]=useState<any>(),[hsdsFrame,setHsdsFrame]=useState<any>(),[panel,setPanel]=useState<'chat'|'settings'>('chat');const profile=useMemo(()=>detectDevice(),[]),iframe=useRef<HTMLIFrameElement>(null),fileInput=useRef<HTMLInputElement>(null),hsdsTimer=useRef<number|undefined>(undefined)
 const load=useCallback(()=>api.divineProjects().then(result=>setProjects(result.data)).catch(error=>notify(error instanceof Error?error.message:'Falha ao carregar Divine Engine')),[notify]);useEffect(()=>{void load();api.divineSettingsRegistry().then(setRegistry).catch(()=>undefined)},[load]);useEffect(()=>()=>{if(previewUrl)URL.revokeObjectURL(previewUrl)},[previewUrl]);useEffect(()=>()=>{if(hsdsTimer.current)window.clearTimeout(hsdsTimer.current)},[])
 const loadPreview=async(detail:any)=>{const build=detail.artifacts.find((file:FileRecord)=>file.mimeType==='text/html');if(!build)return;try{const url=await api.previewFile(build);setPreviewUrl(previous=>{if(previous)URL.revokeObjectURL(previous);return url})}catch(error){notify(error instanceof Error?error.message:'Falha no preview')}}
 const open=async(id:string)=>{try{const detail=await api.divineProject(id);setSelected(detail);const[currentSettings,currentCommands]=await Promise.all([api.divineProjectSettings(id),api.divineCommands(id)]);setSettings(currentSettings);setCommands(currentCommands.data);await loadPreview(detail)}catch(error){notify(error instanceof Error?error.message:'Falha ao abrir projeto')}}
 const create=async()=>{if(!name.trim()||brief.trim().length<10){notify('Informe nome e uma descrição detalhada.');return}setCreating(true);try{const result=await api.createDivineProject({name:name.trim(),brief:brief.trim(),target,executionPolicy:'remote-first',deviceProfile:profile});setSelected(result);setSettings(await api.divineProjectSettings(result.divine.id));setCommands([]);setName('');setBrief('');await load();notify(`Projeto real criado · ${result.pipeline3d.gaps.length} gaps identificados`)}catch(error){notify(error instanceof Error?error.message:'Falha ao criar projeto')}finally{setCreating(false)}}
 const prototype=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildDivinePrototype(selected.divine.projectId,selected.divine.brief,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'));const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();await loadPreview(detail);notify(`Pipeline real concluído · ${result.graph.summary.verified} artifacts verificados`)}catch(error){notify(error instanceof Error?error.message:'Falha no pipeline prototype')}finally{setPrototyping(false)}}
 const experiment4d=async()=>{if(!selected)return;setPrototyping(true);try{await api.buildExperimental4D(selected.divine.projectId,`${selected.divine.name}-4d`);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await loadPreview(detail);await load();notify('Experimental 4D artifact criado · 16 vértices · 32 arestas · projeção matemática')}catch(error){notify(error instanceof Error?error.message:'Falha no experimento 4D')}finally{setPrototyping(false)}}
 const buildCore=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildUesCore(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`UES Core executado · ${Object.keys(result.capabilities.systems).length} sistemas · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha no UES Core')}finally{setPrototyping(false)}}
 const buildMultidimensional=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildUesMultidimensional(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`UES 2D/2.5D/3.5D · ${result.artifacts.length} artifacts verificados`)}catch(error){notify(error instanceof Error?error.message:'Falha multidimensional UES')}finally{setPrototyping(false)}}
 const buildComplement=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.evaluateDThesisComplement({projectId:selected.divine.projectId,name:selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),objective:selected.divine.brief,constraints:['preservar intenção','realismo não obrigatório'],resources:['CPU','SNB','UES'],priorities:{quality:8,performance:7,safety:9,cost:5,scalability:7},hardware:profile.tier==='high'?'high':profile.tier==='low'?'low':'balanced',includeWarScenario:true});const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Tese dos D+ · ${result.summary.domains} domínios · ${result.summary.npcActions} decisões NMN · GPP ${Number(result.summary.gpp).toFixed(1)}`)}catch(error){notify(error instanceof Error?error.message:'Falha no complemento da Tese dos D')}finally{setPrototyping(false)}}
 const buildLiving=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildUesLivingWorld(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief.slice(0,80)||'living');const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Mundo vivo · ${result.result.world.settlements} assentamentos · ${result.result.society.sampleSize} agentes · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha no mundo vivo UES')}finally{setPrototyping(false)}}
 const buildCraft=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildUesCraft(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Craft UES · retopo/anatomy/net/image · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha no craft UES')}finally{setPrototyping(false)}}
 const buildContinuum=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildUesContinuum(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Contínuo UES · cidade ${result.result.city.sampleSize} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha no contínuo UES')}finally{setPrototyping(false)}}
 const buildForge=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildUesForge(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Forja UES · ${result.result.corpus.count} objetos · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha na forja UES')}finally{setPrototyping(false)}}
 const buildEmulation=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildUesEmulation(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Emulação UES · terra/água/TITKO · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha na emulação UES')}finally{setPrototyping(false)}}
 const buildRealis=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildUesRealis(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Realis UES · cadeia dados→D→D-O15 · artifact ${result.artifact.status} · NASA ${result.result.verification.nasa?'sim':'não'}`)}catch(error){notify(error instanceof Error?error.message:'Falha no Realis UES')}finally{setPrototyping(false)}}
 const evaluateGeneration=async()=>{if(!selected)return;setPrototyping(true);try{const[score,ship]=await Promise.all([api.v1Generation(),api.evaluateUesShip(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'))]);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Geração 1 · compete ${score.percent}% · falta ${score.remaining}% · ship ${ship.result.ready?'pronto':'ainda não'} · V1 completa=${score.complete}`)}catch(error){notify(error instanceof Error?error.message:'Falha no critério de geração')}finally{setPrototyping(false)}}
 const buildClose=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildUesClose(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Núcleo UES · 3D aberto ${result.result.semantic3d.kind} · Vulkan ${result.result.verification.vulkanRequired?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha no núcleo UES')}finally{setPrototyping(false)}}
 const buildAtelier=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildUesAtelier(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Atelier UES · CSG ${result.result.solid.subtract} · vidas ${result.result.lives.population} · consenso ${result.result.consensus.decision} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha no atelier UES')}finally{setPrototyping(false)}}
 const buildGenesis=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildUesGenesis(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Gênesis UES · GPU ${result.result.gpu.drawn} · shader ${result.result.shader.optimized} · WebGPU ${result.result.verification.webgpuRequired?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha na Gênesis UES')}finally{setPrototyping(false)}}
 const buildRadiance=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildUesRadiance(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Radiância UES · ${result.result.frame.width}×${result.result.frame.height} · Unreal ${result.result.verification.beatsUnreal?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha na radiância UES')}finally{setPrototyping(false)}}
 const buildRrw=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrw(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`RRW · nós ${result.result.nodes} · pipeline tradicional ${result.result.verification.traditionalPipeline?'sim':'não'} · mesma realidade ${result.result.devices.sameIds?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha no RRW')}finally{setPrototyping(false)}}
 const buildRrwPhenomena=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrwPhenomena(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Fenômenos RRW · catálogo ${result.result.catalog.open?'aberto':'fechado'} · conservação ${result.result.quantities.conserved?'ok':'falha'} · consciência ${result.result.verification.consciousnessClaim?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha nos fenômenos RRW')}finally{setPrototyping(false)}}
 const buildRrwLoop=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrwLoop(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Laço RRW · bioma ${result.result.intent.biome} · Gênesis fechada ${result.result.verification.genesisClosed?'sim':'não'} · mesma realidade ${result.result.devices.sameIds?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha no laço RRW')}finally{setPrototyping(false)}}
 const buildRrwWorld=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrwWorld(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Mundo RRW · hold ${result.result.held.stable?'ok':'falha'} · caminho ${result.result.walk.found?'sim':'não'} · Gênesis fechada ${result.result.verification.genesisClosed?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha no mundo RRW')}finally{setPrototyping(false)}}
 const buildRrwGenesis=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrwGenesis(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Ciclo RRW · noite fria ${result.result.circadian.nightColder?'sim':'não'} · retoma ${result.result.resume.resumed?'sim':'não'} · Gênesis fechada ${result.result.verification.genesisClosed?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha no ciclo RRW')}finally{setPrototyping(false)}}
 const buildRrwChain=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrwChain(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Cadeia RRW · passos ${result.result.steps.length} · executada ${result.result.executed?'sim':'não'} · Gênesis fechada ${result.result.verification.genesisClosed?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha na cadeia RRW')}finally{setPrototyping(false)}}
 const buildRrwHabit=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrwHabit(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Habitar RRW · abrigo ${result.result.studio.hasShelter?'sim':'não'} · cidade ${result.result.inhabited.city.population} · Gênesis fechada ${result.result.verification.genesisClosed?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha ao habitar RRW')}finally{setPrototyping(false)}}
 const buildRrwContinue=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrwContinue(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Segue RRW · dias ${result.result.time.days} · retoma ${result.result.continued.resumed?'sim':'não'} · Gênesis fechada ${result.result.verification.genesisClosed?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha ao continuar RRW')}finally{setPrototyping(false)}}
 const buildRrwChronicle=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrwChronicle(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Crônica RRW · lembrou fogo ${result.result.session.fireRemembered?'sim':'não'} · forrage ${result.result.session.forageRemembered?'sim':'não'} · Gênesis fechada ${result.result.verification.genesisClosed?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha na crônica RRW')}finally{setPrototyping(false)}}
 const buildRrwLive=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrwLive(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Viver RRW · horas ${result.result.hours} · fogo esfriou ${result.result.fireCooled?'sim':'não'} · Gênesis fechada ${result.result.verification.genesisClosed?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha no tique vivo RRW')}finally{setPrototyping(false)}}
 const buildRrwPersist=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrwPersist(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Persiste RRW · mundo ${result.result.worldId} · fogo esfriou ${result.result.fireCooled?'sim':'não'} · recarregado ${result.result.reloaded?'sim':'não'} · Gênesis fechada ${result.result.verification.genesisClosed?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha na persistência RRW')}finally{setPrototyping(false)}}
 const buildRrwUnify=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrwUnify(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Unir RRW · parentesco ${result.result.kin.bound?'sim':'não'} · trabalho ${result.result.labor.worked?'sim':'não'} · Gênesis fechada ${result.result.verification.genesisClosed?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha na unificação RRW')}finally{setPrototyping(false)}}
 const buildRrwDepth=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrwDepth(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Fundo RRW · muda ${result.result.sapling?'sim':'não'} · carbono ${result.result.cycles.carbon?'ok':'não'} · Gênesis fechada ${result.result.verification.genesisClosed?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha no fundo RRW')}finally{setPrototyping(false)}}
 const buildRrwEarth=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildRrwEarth(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Terra RRW · sal ${result.result.earth.oceanSaltier?'ok':'não'} · erosão ${result.result.earth.eroded?'sim':'não'} · Gênesis fechada ${result.result.verification.genesisClosed?'sim':'não'} · artifact ${result.artifact.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha na Terra RRW')}finally{setPrototyping(false)}}
 const buildAdvanced=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.buildUesAdvanced(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),selected.divine.brief);const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`UES Advanced executado · ${result.summary.parts} partes · ${result.summary.triangles} triângulos · ${result.summary.criticFindings} findings`)}catch(error){notify(error instanceof Error?error.message:'Falha no pipeline avançado UES')}finally{setPrototyping(false)}}
 const release=async()=>{if(!selected)return;setPrototyping(true);try{const result=await api.releasePackage(selected.divine.projectId,selected.divine.name.replace(/[^a-zA-Z0-9_-]/g,'-'),'0.1.0-beta','Verified UES prototype package.');const detail=await api.divineProject(selected.divine.id);setSelected(detail);await load();notify(`Release real criado · ${result.manifest.artifacts.length} artifacts · ${result.file.name}`)}catch(error){notify(error instanceof Error?error.message:'Falha no release package')}finally{setPrototyping(false)}}
 const runtimePatch=(values:Record<string,unknown>)=>iframe.current?.contentWindow?.postMessage({type:'snb-runtime-patch',rotationSpeed:Number(values['rendering.rotation-speed']??.005),color:[Number(values['rendering.background-red']??25)/100,Number(values['rendering.background-green']??55)/100,Number(values['rendering.background-blue']??95)/100],autoRotate:Boolean(values['camera.auto-rotate']??true),arrowControl:Boolean(values['input.arrow-control']??true),reducedMotion:Boolean(values['accessibility.reduced-motion']??false)},'*')
 const updateSetting=async(key:string,value:unknown)=>{if(!selected)return;try{const next=await api.updateDivineProjectSettings(selected.divine.id,{[key]:value});setSettings(next);runtimePatch(next.values);notify(`${key} atualizado · ${registry.settings.find((item:any)=>item.key===key)?.status}`)}catch(error){notify(error instanceof Error?error.message:'Falha na configuração')}}
 const applyPreset=async(preset:string)=>{if(!selected)return;try{const next=await api.applyDivinePreset(selected.divine.id,preset);setSettings(next);runtimePatch(next.values);notify(`Preset ${preset} aplicado`)}catch(error){notify(error instanceof Error?error.message:'Falha no preset')}}
 const attach=async(event:ChangeEvent<HTMLInputElement>)=>{const file=event.target.files?.[0];if(!file||!selected)return;try{const uploaded=await api.uploadFile(file,selected.divine.projectId);setAttachmentIds(ids=>[...ids,uploaded.id]);notify(`${uploaded.name} anexado com SHA-256`)}catch(error){notify(error instanceof Error?error.message:'Falha no anexo')}finally{event.target.value=''}}
 const send=async()=>{if(!selected||!message.trim())return;try{const command=await api.sendDivineCommand(selected.divine.id,message.trim(),attachmentIds);setCommands(items=>[...items,command]);setMessage('');setAttachmentIds([]);if(command.operations.length){const next=await api.divineProjectSettings(selected.divine.id);setSettings(next);runtimePatch(next.values)}notify(command.status==='applied'?'Runtime patch aplicado sem reload':'Comando registrado para planejamento da Master AI')}catch(error){notify(error instanceof Error?error.message:'Falha no comando')}}
 const stopHsds=async()=>{if(hsdsTimer.current)window.clearTimeout(hsdsTimer.current);hsdsTimer.current=undefined;if(hsds?.session?.id)await api.closeHsds(hsds.session.id).catch(()=>undefined);setHsds(undefined);setHsdsFrame(undefined)}
 const startHsds=async()=>{if(!selected)return;await stopHsds();try{const bandwidth=profile.network.effectiveType==='4g'?12:profile.network.effectiveType==='3g'?3:profile.network.effectiveType==='2g'?0.7:5,opened=await api.createHsdsSession(selected.divine.id,{viewportWidth:Math.min(innerWidth,1024),viewportHeight:Math.min(innerHeight,1024),bandwidthMbps:bandwidth,decodeTier:profile.tier as 'low'|'balanced'|'high',saveData:profile.network.saveData});setHsds(opened);let active=true;const pull=async()=>{if(!active)return;try{const frames=await api.hsdsFrames(opened.session.id);if(frames.length)setHsdsFrame(frames.at(-1))}catch{active=false;setHsds(undefined);return}hsdsTimer.current=window.setTimeout(pull,Math.max(250,Math.round(3000/opened.session.profile.fps)))};void pull();notify(`HSDS real iniciado · SVG/SSE ${opened.session.profile.width}×${opened.session.profile.height}`);return()=>{active=false}}catch(error){notify(error instanceof Error?error.message:'Falha ao iniciar HSDS')}}
 const hsdsKey=async(key:string)=>{if(!hsds)return;try{const result=await api.hsdsInput(hsds.session.id,{type:'keyboard',key});setHsdsFrame((value:any)=>value?{...value,meta:result.feedbackFrame}:value)}catch(error){notify(error instanceof Error?error.message:'Falha no canal de entrada HSDS')}}
 const categories=[...new Set(registry.settings.map((item:any)=>item.category))] as string[],filtered=registry.settings.filter((item:any)=>(!settingCategory||item.category===settingCategory)&&`${item.label} ${item.key}`.toLowerCase().includes(settingSearch.toLowerCase())).slice(0,visibleLimit)
 return <div className={`divine-page device-${profile.tier}`}><header className="divine-hero"><div className="divine-brand"><span><Gem size={25}/></span><div><small>SNB INTELLIGENCE-FIRST CREATION</small><h1>Divine Engine</h1><p>Palco real, chat, presets e configuração manual sem engine pesada no dispositivo.</p></div></div><div className="device-terminal"><Smartphone size={18}/><div><strong>Device = terminal</strong><span>{profile.tier} · {profile.cores} cores · {profile.network.effectiveType}</span></div><i/></div></header><section className="divine-layout"><aside className="divine-create"><span className="kicker">NOVA PRODUÇÃO</span><h2>O que vamos construir?</h2><input value={name} onChange={e=>setName(e.target.value)} placeholder="Nome do projeto"/><textarea value={brief} onChange={e=>setBrief(e.target.value)} placeholder="Jogo, app, site, software, simulação, cena ou experiência..."/><label>Destino<select value={target} onChange={e=>setTarget(e.target.value)}>{['web','desktop','mobile','unity','unreal','godot','roblox','custom'].map(value=><option key={value}>{value}</option>)}</select></label><button onClick={create} disabled={creating}>{creating?<RefreshCw className="spin" size={16}/>:<WandSparkles size={16}/>}Compilar produção</button><div className="remote-principle"><Cloud size={17}/><p><strong>Remote-first</strong><span>O dispositivo recebe estado, artifacts e preview progressivo.</span></p></div></aside><main className="divine-main"><div className="divine-stats"><div><Settings2 size={18}/><strong>{registry.summary.total??220}</strong><span>Opções tipadas</span></div><div><Layers3 size={18}/><strong>{registry.presets.length||10}</strong><span>Presets</span></div><div><Box size={18}/><strong>{projects.reduce((sum,item)=>sum+item.artifactCount,0)}</strong><span>Artifacts reais</span></div><div><Server size={18}/><strong>{profile.tier==='low'?'lite':'progressive'}</strong><span>Interface</span></div></div><div className="divine-projects">{projects.map(project=><button key={project.id} className={selected?.divine.id===project.id?'active':''} onClick={()=>open(project.id)}><span className="divine-project-icon"><Layers3 size={18}/></span><div><strong>{project.name}</strong><small>{project.target} · {project.status}</small><i><b style={{width:`${project.progress}%`}}/></i></div><em>{project.progress}%</em><ChevronRight size={14}/></button>)}{!projects.length&&<div className="real-data-empty"><strong>Nenhuma produção</strong><p>Crie um projeto real; nenhum progresso será simulado.</p></div>}</div>{selected&&<section className="divine-studio"><div className="divine-stage"><header><div><Radio size={14}/><strong>{selected.divine.name}</strong><span>{selected.divine.status}</span></div><div className="stage-actions"><button onClick={prototype} disabled={prototyping}>{prototyping?<RefreshCw className="spin" size={13}/>:<Play size={13}/>}Build prototype</button><button onClick={experiment4d} disabled={prototyping}><Gem size={13}/>4D Lab</button><button onClick={buildCore} disabled={prototyping}><Cpu size={13}/>UES Core</button><button onClick={buildAdvanced} disabled={prototyping}><WandSparkles size={13}/>Advanced</button><button onClick={buildMultidimensional} disabled={prototyping}><Layers3 size={13}/>2D→3.5D</button><button onClick={buildComplement} disabled={prototyping}><Users size={13}/>D+ Realidade</button><button onClick={buildLiving} disabled={prototyping}><Server size={13}/>Mundo vivo</button><button onClick={buildCraft} disabled={prototyping}><Box size={13}/>Craft+</button><button onClick={buildContinuum} disabled={prototyping}><Layers3 size={13}/>Contínuo</button><button onClick={buildForge} disabled={prototyping}><WandSparkles size={13}/>Forja</button><button onClick={buildEmulation} disabled={prototyping}><Globe size={13}/>Emulação</button><button onClick={buildRealis} disabled={prototyping}><Cloud size={13}/>Realis</button><button onClick={buildClose} disabled={prototyping}><Cpu size={13}/>Núcleo</button><button onClick={buildAtelier} disabled={prototyping}><Box size={13}/>Atelier</button><button onClick={buildGenesis} disabled={prototyping}><Gem size={13}/>Gênesis</button><button onClick={buildRadiance} disabled={prototyping}><Gem size={13}/>Radiância</button><button onClick={buildRrw} disabled={prototyping}><Globe size={13}/>RRW</button><button onClick={buildRrwPhenomena} disabled={prototyping}><Globe size={13}/>Fenômenos</button><button onClick={buildRrwLoop} disabled={prototyping}><Globe size={13}/>Laço</button><button onClick={buildRrwWorld} disabled={prototyping}><Globe size={13}/>Mundo</button><button onClick={buildRrwGenesis} disabled={prototyping}><Globe size={13}/>Ciclo</button><button onClick={buildRrwChain} disabled={prototyping}><Globe size={13}/>Cadeia</button><button onClick={buildRrwHabit} disabled={prototyping}><Globe size={13}/>Habitar</button><button onClick={buildRrwContinue} disabled={prototyping}><Globe size={13}/>Segue</button><button onClick={buildRrwChronicle} disabled={prototyping}><Globe size={13}/>Crônica</button><button onClick={buildRrwLive} disabled={prototyping}><Globe size={13}/>Viver</button><button onClick={buildRrwPersist} disabled={prototyping}><Globe size={13}/>Persiste</button><button onClick={buildRrwUnify} disabled={prototyping}><Globe size={13}/>Unir</button><button onClick={buildRrwDepth} disabled={prototyping}><Globe size={13}/>Fundo</button><button onClick={buildRrwEarth} disabled={prototyping}><Globe size={13}/>Terra</button><button onClick={evaluateGeneration} disabled={prototyping}><ShieldCheck size={13}/>Geração</button><button onClick={release} disabled={prototyping}><Package size={13}/>Release</button><button onClick={hsds?stopHsds:startHsds}><Radio size={13}/>{hsds?'Stop HSDS':'HSDS Live'}</button></div></header><div className="stage-frame">{hsdsFrame?<div className="hsds-player" tabIndex={0} onKeyDown={event=>{if(['ArrowLeft','ArrowRight','ArrowUp','ArrowDown'].includes(event.key)){event.preventDefault();void hsdsKey(event.key)}}}><img src={`data:${hsdsFrame.meta.mimeType};base64,${hsdsFrame.data}`} alt="HSDS live visual state"/><div><b>LIVE · {hsdsFrame.meta.codec}/{hsdsFrame.meta.transport}</b><span>frame {hsdsFrame.meta.sequence} · SHA-256 {hsdsFrame.meta.checksum.slice(0,10)}…</span></div></div>:previewUrl?<iframe ref={iframe} src={previewUrl} sandbox="allow-scripts" title="Divine real build preview" onLoad={()=>runtimePatch(settings.values)}/>:<div className="stage-placeholder"><Monitor size={32}/><strong>Palco aguardando artifact real</strong><span>Crie o pipeline prototype; nenhum preview falso será exibido.</span></div>}</div><footer><span><Activity size={12}/>{selected.artifacts.length} artifacts</span><span><ShieldCheck size={12}/>{selected.pipeline3d.gaps.length} gaps</span><span><Cpu size={12}/>runtime patch nativo</span></footer></div><aside className="divine-side"><nav><button className={panel==='chat'?'active':''} onClick={()=>setPanel('chat')}><MessageSquareText size={14}/>Chat</button><button className={panel==='settings'?'active':''} onClick={()=>setPanel('settings')}><Settings2 size={14}/>Config</button></nav>{panel==='chat'?<div className="divine-chat"><div className="chat-log">{commands.map(command=><article key={command.id}><strong>Você</strong><p>{command.message}</p><small>{command.status} · {command.attachmentFileIds.length} anexos</small>{command.operations?.[0]?.explanation&&<details><summary>Como isso funciona</summary><p>{command.operations[0].explanation.what}<br/>{command.operations[0].explanation.how}<br/>Manual: {command.operations[0].explanation.manual}</p></details>}</article>)}{!commands.length&&<div className="stage-placeholder"><MessageSquareText size={22}/><strong>Direção em tempo real</strong><span>Use /set rendering.rotation-speed=0.02 para um patch nativo imediato.</span></div>}</div>{attachmentIds.length>0&&<div className="attached-count"><Paperclip size={12}/>{attachmentIds.length} arquivo(s) anexado(s)<button onClick={()=>setAttachmentIds([])}><X size={11}/></button></div>}<div className="chat-entry"><textarea value={message} onChange={e=>setMessage(e.target.value)} placeholder="Peça uma alteração ou use /set key=value"/><input ref={fileInput} type="file" hidden onChange={attach}/><button onClick={()=>fileInput.current?.click()}><FileUp size={14}/></button><button onClick={send}><Send size={14}/></button></div></div>:<div className="divine-settings"><div className="preset-strip">{registry.presets.map((preset:any)=><button className={settings.preset===preset.id?'active':''} onClick={()=>applyPreset(preset.id)} key={preset.id}>{preset.name}</button>)}</div><label className="settings-search"><Search size={13}/><input value={settingSearch} onChange={e=>setSettingSearch(e.target.value)} placeholder="Buscar entre 220 opções"/></label><select value={settingCategory} onChange={e=>{setSettingCategory(e.target.value);setVisibleLimit(profile.tier==='low'?20:40)}}>{categories.map(category=><option key={category}>{category}</option>)}</select><div className="settings-list">{filtered.map((item:any)=>{const value=settings.values[item.key]??item.default;return <label key={item.key}><span><strong>{item.label}</strong><small>{item.key} · {item.status}</small></span>{item.type==='boolean'?<input type="checkbox" checked={Boolean(value)} onChange={e=>updateSetting(item.key,e.target.checked)}/>:<input type="number" value={Number(value)} min={item.min} max={item.max} step={item.step} onChange={e=>updateSetting(item.key,Number(e.target.value))}/>}</label>})}</div>{filtered.length<registry.settings.filter((item:any)=>item.category===settingCategory).length&&<button className="load-settings" onClick={()=>setVisibleLimit(value=>value+20)}>Mostrar mais opções</button>}</div>}</aside></section>}</main></section></div>
}
