import{createHash}from'node:crypto';const esc=(v:string)=>v.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&apos;'}[c]!));export const createSvgScene=(title:string,prompt:string,width=1024,height=576)=>{const seed=parseInt(createHash('sha256').update(prompt).digest('hex').slice(0,8),16),hue=seed%360,objects=Array.from({length:24},(_,i)=>{const x=(seed*(i+3)*17)%width,y=height*.58+(seed*(i+7)%180),r=12+(seed*(i+11)%45);return`<circle cx="${x}" cy="${y}" r="${r}" fill="hsl(${(hue+i*13)%360} 55% ${35+i%4*8}%)" opacity=".78"/>`}).join('');return`<svg xmlns="http://www.w3.org/2000/svg" width="${width}" height="${height}" viewBox="0 0 ${width} ${height}" role="img" aria-label="${esc(title)}"><defs><linearGradient id="sky" y2="1"><stop stop-color="hsl(${hue} 55% 18%)"/><stop offset="1" stop-color="hsl(${(hue+55)%360} 65% 48%)"/></linearGradient></defs><rect width="100%" height="100%" fill="url(#sky)"/><path d="M0 ${height*.65} Q${width*.25} ${height*.35} ${width*.5} ${height*.65}T${width} ${height*.6}V${height}H0Z" fill="#111827" opacity=".7"/><g id="semantic-objects">${objects}</g><text x="40" y="64" fill="white" font-family="system-ui" font-size="32">${esc(title)}</text><metadata>${esc(JSON.stringify({format:'ues-2d-scene-v1',promptHash:createHash('sha256').update(prompt).digest('hex'),objects:24}))}</metadata></svg>`}
