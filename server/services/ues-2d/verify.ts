export const verifySvg=(value:string)=>({valid:value.startsWith('<svg')&&value.includes('<metadata>')&&value.includes('semantic-objects'),objects:(value.match(/<circle /g)??[]).length,noScripts:!value.includes('<script')});export const verifyParallax=(value:string)=>({valid:value.startsWith('<!doctype html>')&&value.includes('pointermove')&&value.includes('perspective:800px'),networkRequests:0,interactive:true});export const verifyHybrid=(value:any)=>({valid:value?.format==='ues-3.5d-experimental-v1'&&value.layers?.length===3,classification:value.classification,physicalClaim:value.claims?.physicalDimensionDiscovery===true})
