import{createHash}from'node:crypto';import{mkdir,writeFile}from'node:fs/promises';import{join,resolve}from'node:path';import type{FileAsset}from'../../domain.js';import{id,now}from'../../lib/id.js';import type{Store}from'../../repositories/store.js';import type{ArtifactGraphService}from'../artifact-graph.js';import{DThesisCore}from'./core.js';import{dRegistry}from'./registry.js';import type{DContext}from'./types.js';export class DThesisService{private core=new DThesisCore();constructor(private store:Store,private artifacts:ArtifactGraphService,private root=resolve('./data/uploads')){}registry(){return{thesis:'Tese dos D',physicalDimensions:false,roadmapLevels:false,allAvailableInV1:true,dO15:'transversal quality-preserving optimization',data:dRegistry}}async evaluate(userId:string,input:{projectId:string;name:string;context:DContext}){this.store.getProject(input.projectId,userId);const evaluation=this.core.evaluate(input.context),bytes=Buffer.from(JSON.stringify(evaluation,null,2)),dir=join(this.root,userId);await mkdir(dir,{recursive:true});const fileId=id('file'),name=`${input.name.replace(/[^a-zA-Z0-9._-]/g,'_')}-d-thesis.json`,storagePath=join(dir,`${fileId}-${name}`);await writeFile(storagePath,bytes,{flag:'wx'});const file:FileAsset={id:fileId,userId,projectId:input.projectId,name,mimeType:'application/vnd.snb.d-thesis+json',size:bytes.length,checksum:createHash('sha256').update(bytes).digest('hex'),storagePath,createdAt:now()};this.store.createFile(file);const artifact=this.artifacts.register(userId,{projectId:input.projectId,fileId:file.id,type:'analysis.d-thesis',producer:'snb-ues.d-thesis',verification:{valid:evaluation.selectedDs.length>0&&evaluation.gpp.score>=0,dCount:dRegistry.length,allAvailable:evaluation.governance.allDsAvailable,absolutePerfectionClaim:false},metadata:{selectedDs:evaluation.selectedDs.map(x=>x.key),gppScore:evaluation.gpp.score,do15Accepted:evaluation.dO15.decisions.filter(x=>x.accepted).length},license:'Apache-2.0'});return{evaluation,file:{...file,storagePath:undefined},artifact}}}
