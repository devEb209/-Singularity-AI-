import{candidateScore}from'./perfect-point.js';import type{DCandidate,DContext}from'./types.js';export const refine=(candidate:DCandidate,context:DContext)=>{const max=Math.max(1,Math.min(context.maxIterations??4,8)),iterations=[],current={...candidate,metrics:{...candidate.metrics}};let previous=candidateScore(current,context);for(let i=1;i<=max;i++){current.metrics.stability=Math.min(100,current.metrics.stability+2);current.metrics.quality=Math.min(100,current.metrics.quality+1);const score=candidateScore(current,context),gain=score-previous;iterations.push({iteration:i,score,gain:Number(gain.toFixed(3)),action:'critic-guided stability and quality refinement'});previous=score;if(gain<.15)break}return{candidate:current,iterations,stoppedBecause:iterations.at(-1)!.gain<.15?'marginal gain below threshold':'iteration budget reached'}}
