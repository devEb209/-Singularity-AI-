import{createHash}from'node:crypto';import{generateCandidates}from'./candidates.js';import{optimizeDO15}from'./optimizer.js';import{candidateScore,localPerfectPoints}from'./perfect-point.js';import{refine}from'./refinement.js';import{selectDs}from'./selector.js';import type{DContext}from'./types.js';export class DThesisCore{evaluate(context:DContext){const selectedDs=selectDs(context),candidates=generateCandidates(context).map(candidate=>({...candidate,score:candidateScore(candidate,context)})).sort((a,b)=>b.score-a.score),initial=candidates[0],localPP=localPerfectPoints(selectedDs.map(x=>x.key),initial,context),optimization=optimizeDO15(initial,context),refinement=refine(optimization.optimized,context),gppScore=candidateScore(refinement.candidate,context),gpp={candidate:refinement.candidate.id,score:gppScore,contextHash:createHash('sha256').update(JSON.stringify(context)).digest('hex'),tradeoffs:localPP.filter(x=>x.localScore<gppScore).map(x=>({d:x.d,local:x.localScore,global:gppScore,reason:'local sacrifice may improve global weighted balance'})),dynamic:true,absolutePerfectionClaim:false};return{format:'snb-ues-d-thesis-v1',thesis:'functional reasoning matrix; not physical or mathematical dimensions',context,selectedDs,candidates,localPerfectPoints:localPP,dO15:optimization,refinement,gpp,governance:{allDsAvailable:true,allDsRequiredPerTask:false,userControl:true,autonomousByDefault:true,recalculateOnNewInformation:true}}}}
