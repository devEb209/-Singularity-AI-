import type{BenchmarkProposal}from'./types.js';export const critiqueProposal=(proposal:BenchmarkProposal)=>{const findings=[];if(proposal.cases.length<2)findings.push('insufficient-case-diversity');if(new Set(proposal.cases.map(x=>x.prompt)).size!==proposal.cases.length)findings.push('duplicate-prompts');if(proposal.cases.some(x=>x.criteria.length<3))findings.push('weak-criteria');if(!proposal.cases.some(x=>x.adversarial))findings.push('missing-adversarial-case');return{valid:findings.length===0,findings}}
