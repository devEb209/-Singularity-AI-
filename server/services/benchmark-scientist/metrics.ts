import type{ModelEvaluation}from'../../domain.js';export const evaluationMetrics=(items:ModelEvaluation[])=>{const scores=items.map(x=>x.score),mean=scores.length?scores.reduce((a,b)=>a+b,0)/scores.length:0,std=Math.sqrt(scores.reduce((sum,value)=>sum+(value-mean)**2,0)/(scores.length||1)),latencies=items.flatMap(x=>x.latencyMs===undefined?[]:[x.latencyMs]).sort((a,b)=>a-b),p95=latencies[Math.max(0,Math.ceil(latencies.length*.95)-1)]??0,success=items.filter(x=>x.success).length/(items.length||1);return{evidence:items.length,successRate:Number(success.toFixed(4)),scoreMean:Number(mean.toFixed(2)),scoreStdDev:Number(std.toFixed(2)),latencyP95:p95}}
