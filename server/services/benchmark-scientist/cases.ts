import type{Capability}from'../../domain.js';const prompts:Record<Capability,string[]>= {chat:['Resolve ambiguity before answering','Maintain context without inventing facts'],reasoning:['Compare three hypotheses and falsify one','Solve a constrained multi-step deduction'],code:['Implement and test an edge-case parser','Find a concurrency and authorization defect'],research:['Find two independent current sources','Identify contradiction between primary sources'],vision:['Describe measurable visual relations','Detect inconsistent geometry in a reference'],creative:['Create under strict style and safety constraints','Revise a draft from critic findings'],planning:['Produce a dependency-valid rollback plan','Replan after a failed prerequisite']};export const generateCases=(capability:Capability,weaknesses:string[])=>prompts[capability].map((prompt,index)=>({id:`generated-${capability}-${index+1}`,prompt:`${prompt}. Focus: ${weaknesses.join(', ')||'coverage'}.`,criteria:['constraint compliance','verifiable output','explicit uncertainty','no fabricated execution'],adversarial:index===1}))
