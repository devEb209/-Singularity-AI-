import{describe,expect,it}from'vitest';import{SQLiteStore}from'../repositories/sqlite-store.js';import{WorkerCoordinator}from'./worker-runtime.js';import{SelfDiagnosticsService}from'./self-diagnostics.js';describe('SNB self diagnostics',()=>it('reports real state and limits healing to safe maintenance',()=>{const store=new SQLiteStore(':memory:'),workers=new WorkerCoordinator(store,60),service=new SelfDiagnosticsService(store,workers,'secret'),report=service.inspect('u');expect(report.findings.some(x=>x.code==='NO_ONLINE_WORKER')).toBe(true);expect(report.receipt).toMatch(/^snb-diagnostics-hmac:/);const repair=service.repair('u');expect(repair.hostModified).toBe(false);expect(repair.actions.map(x=>x.action)).toEqual(['recover-expired-leases','expire-stale-memory-versions']);store.close()}))
